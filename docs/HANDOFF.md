# HANDOFF — career 스킬 개발

> 스킬 **개발** 세션의 인수인계 (비-PII, 모드 B — `main` 머지로 어느 세션이든 `git pull`로 이어받음).
> 사용자 개인 취업 상태는 여기 없다 → 그건 개인 저장소의 `.private/session-state.md`(모드 C).
> 규율: [`reference/handoff.md`](../reference/handoff.md)

---

## Latest — 2026-07-26 / 경계 게이트 + 윤문 룰북 내재화 (검증 지적 28건 전량 반영)

### 어디까지 왔나

두 갈래를 끝냈다. **로직 중복 재발 차단**과 **취업 특화 윤문 룰북 내재화**.
후자는 적대 검증에서 28건(치명 5 + 나머지 23)이 나왔고 **전량 반영**했다.

| 갈래 | 결과 |
|---|---|
| 중복 재발 차단 | `check_workspace.sh`(스킬 소유·워크스페이스 인자) + smoke 집계 편입 |
| 윤문 룰북 | `polish-rulebook.md` + `check_polish.py`. 반전 15건이 핵심 |
| 검증 28건 | 치명 5 + 나머지 23 **전량 반영** |

### 중복은 규칙이 아니라 구조 문제였다

실측 피해: `discover.py` 사본이 남아 거짓 기각 수정이 한쪽에만 들어갔고,
**사용자의 실동 도구가 진짜 본사 공고를 놓치는 상태**였다. `jd-filter-simon.html`도 포크돼 있었다.

포크의 **원인**은 템플릿에 불러오기가 없었던 것 — 조건을 저장하려면 파일을 복사할 수밖에 없었다.
원인을 없애고(왕복 추가), 사본을 래퍼로 회수하고, 게이트로 재발을 막았다.

> **경계: 스킬은 경로를 인자로 받는 로직을 소유하고, 워크스페이스는 그 경로가 가리키는 데이터를 소유한다.**
> 판별은 교체 시험 — 남의 값으로 바꿔도 그대로면 로직.

### 윤문 룰북 — 반전 15건이 본체

71패턴을 분류하니 **반전필요 15건**. 원본은 "두괄식 해체·불릿 산문화·헤딩 제거·조사 복원"을
처방하는데 전부 국내 취업 문서 규범의 정반대다. 그대로 이식했으면 이력서가 붕괴한다.

**판정은 변경률이 아니라 보존 지표가 한다.** 변경률은 "경험을 통째로 갈아엎는" 사고를 못 잡고,
반대로 이력서 종결 통일 같은 정상 작업을 중단시킨다. 중단 사유는 수치 신규 삽입(=허위 기재)·
수치/고유명사/출처라벨/JD키워드 소실뿐이다.

### 이번에 확립된 핵심 원칙

13. **경계는 검사 가능한 판별식으로 쓴다** — "스킬=로직, 개인=데이터"는 강제가 안 된다.
    "**경로를 인자로 받는가**"로 바꾸면 grep으로 잡힌다.
14. **검사기는 자기 원칙을 자신에게도 적용한다** — 개인 저장소 실행 로직을 금지하는 검사기가
    `.private/` 안에 있으면 자기모순이다. PII 목록도 마찬가지 — **목록은 데이터**라
    비공개 쪽에 두고 로직만 공개 쪽에 둔다. 아니면 PII를 지키려고 PII를 커밋하게 된다.
15. **폴백은 규칙이 많은 쪽이 아니라 오탐 비용이 낮은 쪽** — 축 미상 산출물을 자소서로
    폴백하면 리포트에 자소서 규칙이 걸려 오탐이 쏟아진다.
16. **검사가 유출 경로가 되지 않게** — 위반 보고에 PII·경로 원문을 찍지 않는다.

### 검증

- `smoke_check.sh` **175 passed / 0 failed** (교차 저장소 게이트 편입)
- 음성 대조 7건 — 개인 저장소 로직 부활 · `normalize()` 우회 · 티 토큰 중복 · 임계 불일치 ·
  이스케이프 되돌리기 · 미분류 템플릿 · PII 유입 → **전부 해당 게이트가 정확히 실패**
- 동작 실증 — 축 미지정 exit 3 · 수치 신규삽입/소실 exit 2 · JD 키워드 소실 exit 2 ·
  이력서 종결통일 exit 0(예전 설계면 중단됐다) · 규범 준수 자소서 종결일관성 1.0 ·
  기준축 불명 시 방향별 보수 판정 · 마커 제외 글자수 · 폰트 없으면 exit 2 + 파일 미변경

### 미결 (하나)

- **모드 C 기본값화 범위** — 실사용 관찰이 근거인데 이번에도 실사용 라운드가 없었다.
  근거 없이 정하면 제1 정책을 어기므로 의도적으로 남긴다. 현 상태 유지 — 제안하되 동의 필수.
- 저장소 **LICENSE 미정** — 공개 배포 조건은 소유자 결정. `NOTICE`에 미정 사실만 적어 뒀다.

---

## 이전 — 2026-07-26 / `discover.py` 스킬 승격 + 미결 3건 종결

### 어디까지 왔나

`discover.py`의 귀속을 **스킬 승격**으로 확정하고, 승격에 딸린 미결을 함께 닫았다.
승격의 전제는 하나다 — **개인 값은 전부 필터 입력에서 온다.** 이 전제를 문장이 아니라 게이트로 박았다.

| 미결 | 결말 |
|---|---|
| `discover.py`의 귀속 | **닫힘** — [`scripts/discover.py`](../scripts/discover.py)로 승격. 개인 값 기본값 0, 필터 없으면 중단 |
| `interview-prep`의 `summary` 리치텍스트 가정 | **닫힘** — `metrics:[{k,v}]` + `flag` **구조화 데이터**로 전환. 강조는 데이터가 아니라 렌더러가 넣는다 → 가정 자체가 사라졌고 XSS 게이트의 예외도 없어졌다 |
| `hub.html` 임포트 신뢰 경계 | **닫힘** — 외부 유입 3경로(localStorage·붙여넣기·BYO 백엔드)가 **`normalize()` 한 관문**만 통과하도록 정리하고, 대입문 형태를 게이트로 강제 |
| 모드 C 기본값화 범위 | **못 닫음** — 아래 참조 |

### 큐 A·B 처리 결과

- **A (⭐ 필터 자동 소비) — 닫힘.** `discover.py --filter <경로>`가 `career/jd-filter@1`을 **그대로** 먹는다.
  사용자가 폼에서 고른 조건이 손을 거치지 않고 쿼리 매트릭스가 된다. 스키마 계약은 게이트가 지킨다.
- **B (자사 채용홈 절차 승격) — 부분.** 정직하게 적는다:
  - **승격됨** — ATS 식별(7종 패턴 → 공개 검색 경로), 타깃 기업 **정확 일치** 조회, 3-상태 원장, 커버리지 원장
  - **승격 안 됨(의도)** — 임의 회사 채용홈 크롤링. 사이트마다 달라 범용화가 안 된다.
    대신 **`agent_todo`로 배출**한다 — 스크립트가 조용히 빠뜨리는 게 아니라 *남은 일을 넘긴다*(취득 책임 원칙).

### 이번에 확립된 핵심 원칙

10. **승격의 전제는 게이트로 박는다** — "개인 값은 필터에서 온다"는 문장으로 두면 다음 커밋에 기본값이
    되살아난다. 값이 박히면 실패하고, 필터 없이 돌리면 중단한다. *기본값을 두면 남의 조건으로
    발굴하고도 그런 줄 모른다.*
11. **범용화 못 하는 축은 숨기지 말고 배출한다** — 자동화의 경계를 `agent_todo`로 드러내면
    "자동화됐다"는 착시가 안 생긴다. 빈 목록이 아니라 **비워야 할 목록**이 발굴 종료 조건이다.
12. **리치텍스트 예외는 구조화로 없앤다** — "이 필드만 신뢰한다"는 예외는 신뢰 가정이 깨지는 순간
    구멍이 된다. 강조를 데이터에서 렌더러로 옮기면 예외 자체가 사라진다.

### 검증

- `bash scripts/smoke_check.sh` → **163 passed / 0 failed** (신규 게이트 5건)
- **실사용 스윕 실측** — 제네릭 필터로 라이브 실행: 보드 2쿼리 59건 수집,
  타깃 2개사 중 1개사 본사 5건·1개사 `공고 0건(정상 응답)` → 자사 채용홈 작업목록 배출
- **정확 일치의 효과 실측** — `SK하이닉스` 검색 20건 중 **본사 5건만 채택, 15건 기각**.
  기각된 15건은 전부 다른 회사(협력사·용역)였다. `SK하이닉스㈜`는 법인격 정규화로 정상 채택
- **거짓 기각 보강** — 로고 없는 카드는 폴백이 지역명을 잡는 일이 있어(실측 `"경기 안성시"`)
  **어느 필드든 정규화 완전 일치**면 채택하도록 넓혔다. 노이즈 재유입 0건 확인
- **ATS 경로 실증** — Greenhouse 공개 API 패턴을 실제 호출해 확인(HTTP 200, 418건)
- **음성 대조 3건** — 개인 값 기본값 되살리기 / `normalize()` 우회 대입 / (이전) 이스케이프 되돌리기
  → 셋 다 해당 게이트가 정확히 실패. *가드를 만들었으면 되돌려서 확인한다(원칙 9)*
- **기능 회귀** — 5개 템플릿 렌더 유지, hub 가져오기 왕복 동작(1→2건), 성과 요약 표시 동일

### 미결 (하나 남음)

- **모드 C의 기본값화 범위** — *이번에도 못 닫는다.* 자동 제안 강도는 **실사용 관찰이 근거**인데
  이번 세션은 스킬 개발만 했고 실사용 라운드가 없었다. 근거 없이 정하면 이 저장소의 제1 정책
  ("실측 근거 없는 규칙은 넣지 않는다")을 어기는 것이라 **의도적으로 남긴다.**
  다음 실사용 라운드에서 "사용자가 모드 C를 몇 번 만에 수락/거절했는가"를 세면 그때 닫힌다.
  현재 상태 유지: *제안하되 동의 필수.*

---

## 이전 — 2026-07-26 / 재개 리추얼 검증 중 XSS 게이트 사각지대 발견·차단

### 어디까지 왔나

이전 세션의 **재개 리추얼 자체를 실행해보는 것**에서 시작했다. 두 가지가 드러났다 — 하나는
리추얼이 재현되지 않는다는 것, 다른 하나는 **XSS 게이트가 템플릿 12개 중 2개만 보고 있었다**는 것.
큐 D("`check_xss.js`가 `jd-filter.html`을 검사하는지 확인")를 파고들다 발견했다. **전부 실측 근거가 있다.**

| 발견 | 실측 | 조치 |
|---|---|---|
| 새 컨테이너에서 스모크가 **152/6 실패** | `ModuleNotFoundError: fitz` — PyMuPDF 미설치. 코드 회귀 아님 | `scripts/requirements.txt` 신설 · 사전 점검 배너 · `check_a4.py` 트레이스백 → 실행 가능한 메시지 |
| XSS 게이트가 **2/12 템플릿만** 검사 | `CASES`가 하드코딩 2건 — 나머지는 조용히 미검사 | 검사 7건으로 확대 + **미분류 템플릿 차단 가드** |
| `roadmap.html` **이스케이프 누락 11곳** | `PROFILE.*` 4필드·`arch`·`grade`·`confidence`·`RANK.base`·`interview[]`·`sub[]`(style)·`status`(class) 실측 유출 | 전부 `escHtml`/`escAttr` 적용 |
| `linkedin-export.html` **누락 5곳** | `name`·`group`·`label`·`hint`·`k`(id·onclick 인자) 실측 유출 | 이스케이프 + `escJsAttr` 신설 |
| `cover-letter.html` **textarea 탈출** | `"`만 치환 → 닫는 태그로 시작하는 값이 엘리먼트로 파싱됨 | `escHtml`/`escAttr`로 교체 |
| `interview-prep.html` `tierBadge` 누락 | `class="tier ${t}"`·본문 모두 날값 | 이스케이프 적용 |
| 페이로드 1종의 사각지대 | `<img>` 하나로는 **textarea 본문 탈출을 못 본다** — cover-letter가 그래서 통과했다 | 페이로드 3종(일반·textarea 탈출·속성 탈출)으로 확대 |

### 이번에 확립된 핵심 원칙

7. **게이트의 커버리지도 게이트로 지킨다** — `CASES` 같은 수동 목록은 조용히 뒤처진다.
   새 템플릿은 **검사(CASES) 아니면 면제(EXEMPT, 사유 필수)로 분류되지 않으면 통과 못 한다.**
   휴리스틱 자동 판별을 쓰지 않은 이유: 휴리스틱은 틀려도 조용하다.
8. **페이로드 1종 = 싱크 1종만 검사** — 이스케이프 결함은 싱크 종류마다 다르게 샌다.
   속성·텍스트·textarea 본문은 각각 다른 탈출 문자열을 요구한다.
9. **게이트를 고쳤으면 일부러 되돌려 확인한다** — 이번에 이스케이프를 되돌려 게이트가 실제로
   빨개지는지 확인했다(음성 대조). 통과하는 게이트가 *작동하는* 게이트라는 보장은 없다.

### 검증

- `bash scripts/smoke_check.sh` → **158 passed / 0 failed**
- `node scripts/check_xss.js` → 템플릿 7 × 페이로드 3 통과, 미분류 템플릿 주입 시 exit 2
- **음성 대조**: `PROFILE.role`·`data.a` 이스케이프를 되돌리자 게이트가 정확히 해당 파일·페이로드에서 실패 — cover-letter는 **textarea 탈출 페이로드에서만** 실패했다(3종 확대가 필요했던 이유)
- **기능 회귀**: 수정한 5개 템플릿 실제 렌더 확인(경로 5·스텝 15 / 섹션 5·필드 17 / 항목 3·QA 9 / 문항 3 / 뱅크·지원 1) + 인라인 `onclick` 상호작용 유지(로드맵 필터 5→1, 링크드인 토글 5→6) + `&` 이중 이스케이프 없음

### 미결 / 판단 대기

- **`interview-prep.html`의 `summary`는 의도적 리치텍스트**(`<b>10×</b>`)라 오염 검사에서 뺐다.
  "스킬 생성물이라 신뢰한다"는 가정에 기대고 있다 — 사용자 임포트 경로가 생기면 **구조화 데이터로 바꿔야 한다.**
- `hub.html`은 유일하게 **사용자가 파일로 가져오는** JSON을 받는다(현재 검사 통과). 가져오기 경로가
  늘어나면 신뢰 경계를 문서화할 필요.

---

## 이전 — 2026-07-26 / 실사용 1회로 규칙 6건 도출·머지 (#12–#17)

### 어디까지 왔나

실제 사용자(7년차 생산기술 엔지니어)의 포트폴리오 개선 + 공고 발굴을 **끝까지 수행하면서**, 막힐 때마다 원인을 규칙으로 환원해 스킬에 반영했다. **전부 실측 근거가 붙어 있다** — 추상적 개선이 아니다.

| PR | 내용 | 촉발한 실패 |
|---|---|---|
| #12 | **P7-a 표준 섹션 순서** · **P3-a 엔티티 필드 스키마·결측 인터뷰** | 한 회사만 직급 표기, 한 수상만 수여기관 표기 — 빈칸이 "누락"으로 읽힘 |
| #13 | **§6.0 #8 질문 볼드 표기** · P7-a 순서 개정(역량을 증거 뒤로) | 긴 리포트 속에서 사용자가 질문을 놓침 |
| #14 | **발굴 재현성 규율(§2-2a)** · 현직/협력사 제외 · **`jd-filter.html`** | 같은 발굴이 매번 다른 결과 |
| #15 | **자사 채용홈 전수 조회** · 글로벌 지역별 · 검색엔진 강화 | 대기업 본사 공고를 3회 연속 놓침 |
| #16 | **취득 책임 원칙 · 실패 선언 게이트** · 헤드리스 렌더 단계 | SPA 셸을 보고 "취득 실패"로 오기록 |
| #17 | **핸드오프 모드 C** · **컨텍스트 관리** · 재개 검증 | "PII라서 크로스-컨테이너 재개 불가"라는 낡은 단정 |

### 이번에 확립된 핵심 원칙 (재사용 가치 높음)

1. **취득 책임** — *사용자가 로그인 없이 볼 수 있는 화면이면 나도 가져와야 한다.* "SPA라서"는 종료 사유가 아니라 미완주. → **실패 선언 게이트**로 강제(사다리 1–4 시도 기록 없이 실패 기록 금지)
2. **반복이 아니라 열거** — 결과 불일치의 원인은 무작위성이 아니라 **커버리지 누락**. 같은 탐색을 N번 반복해도 누락은 N번 반복될 뿐. → 소스 레지스트리 × 쿼리 매트릭스 × 커버리지 체크리스트
3. **도구 실패 ≠ 사이트 실패** — 한 도구가 빈 결과를 줬다고 사이트를 단정하지 않는다. 최소 2경로 시도 후 HTTP 코드·본문 크기를 근거로 기록
4. **부분 기재 금지** — 같은 엔티티의 같은 필드는 전부 채우거나 전부 뺀다. **일관된 생략 > 들쭉날쭉한 기재**
5. **주장은 증거 뒤에** — 핵심 역량은 이력·프로젝트 뒤에 둔다
6. **대화는 휘발, 파일은 남는다** — 잃으면 안 되는 것은 생긴 즉시 파일로

### 다음 작업 큐

| # | 작업 | 크기 | 권장 |
|---|---|---|---|
| ~~A~~ | ~~`jd-filter.html` 필터를 `discover` 계열이 자동 소비~~ | — | **완료** — `discover.py --filter`가 `career/jd-filter@1`을 직접 소비. 스키마 계약은 게이트가 지킨다 |
| ~~B~~ | ~~자사 채용홈 조회를 스킬 차원 절차로~~ | — | **부분 완료** — ATS 식별·정확 일치·3-상태 원장 승격. 임의 채용홈 크롤링은 범용화 불가라 `agent_todo`로 배출(위 참조) |
| C | 발굴 보드를 [`templates/jd-discovery.html`](../templates/jd-discovery.html)로 렌더 — 지금은 텍스트 표로만 출력 | small | 템플릿이 이미 있는데 안 쓰임 |
| ~~D~~ | ~~`check_xss.js`가 신규 템플릿(`jd-filter.html`)을 검사 대상에 넣는지 확인~~ | — | **완료** — `jd-filter.html`은 마크업 싱크 0(출력이 `textContent`)이라 **면제가 정답**이었다. 대신 다른 5개 템플릿이 미검사였고 그중 4개에 실제 이스케이프 결함이 있었다(위 표) |

### 미결 / 판단 대기

- **모드 C의 기본값화 범위** — 원격 세션에서 "사실상 기본값"이라고 썼지만, 자동 제안 강도를 어디까지 둘지는 실사용을 더 봐야 한다. 지금은 *제안하되 동의 필수*.
- **`discover.py`의 귀속** — 개인 저장소에 있지만 범용 로직이다. 스킬로 승격할지(큐 B), 개인 도구로 둘지.

### 적용 중인 정책 (영구)

- 실측 근거 없는 규칙은 넣지 않는다. 모든 신규 규칙에 **"어떤 실패가 촉발했는가"** 를 각주로 남긴다.
- 금지선(로그인·페이월·봇차단 우회, 헤더 위조)은 **어떤 강화에서도 완화하지 않는다.** 취득 책임 원칙에도 "잠긴 문을 여는 근거가 아니다"를 명시했다.
- 모든 변경은 `smoke_check.sh` 통과 후 PR → squash 머지.

### 다음 세션 재개

```bash
git fetch origin main && git pull origin main && cat docs/HANDOFF.md
pip install -r scripts/requirements.txt   # ★ 새 컨테이너 필수 — 없으면 A4 게이트 6건 실패(코드 회귀 아님)
bash scripts/smoke_check.sh               # 175 passed / 0 failed 기대
node scripts/check_xss.js                 # exit 0 기대 (템플릿 7 × 페이로드 3)
python3 scripts/discover.py --probe       # 소스 도달성(네트워크 있을 때만)
```

**Fallback:** PR #12–#17 본문에 각 변경의 근거와 검증 결과가 인라인으로 남아 있다 ·
`git log --oneline -8` 로 커밋 메시지에서도 전체 맥락 복원 가능.

# === Private personal context — NEVER commit (D-6, D-8, D-9) ===
.private/
reference/private/

# === Secrets ===
.env
.env.*
*.key
*.pem
credentials.json
.secrets/

# === Extraction / working scratch ===
Gems_Input.zip
_extract/
*.tmp

# === 검증 산출물 (check_a4.py가 입력 옆에 PDF를 생성 — 저장소 오염 방지) ===
samples/*.pdf
templates/*.pdf

# === OS / editor noise ===
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# === Node (if tooling is added later) ===
node_modules/

# === Raw source inputs kept local (IP caution, D-6). Distilled outputs committed: methodology.md, gems/techniques.md ===
reference/gems/Jobs_Verstappen_Gems_SystemPrompt.txt
reference/gems/*.pdf
